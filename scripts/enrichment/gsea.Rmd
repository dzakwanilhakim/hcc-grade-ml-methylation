---
title: "gsea"
output: html_document
date: "2024-06-06"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
go_enrichment <- function(df_sigs){
  original_gene_list <- df_sigs$logFC
  names(original_gene_list) <- df_sigs$gene
  gene_list = sort(original_gene_list, decreasing = TRUE)
  
  organism = "org.Hs.eg.db"
  
  gse_fgsea <- gseGO(geneList=gene_list, 
                     ont ="ALL", 
                     keyType = "SYMBOL", 
                     nPermSimple = 1000000,
                     minGSSize = 5, 
                     maxGSSize = 800, 
                     pvalueCutoff = 0.05, 
                     verbose = TRUE, 
                     OrgDb = organism, 
                     pAdjustMethod = "none",
                     by = 'fgsea')
  return(gse_fgsea)
}
```

```{r}
gsea_NG1_none <- go_enrichment(sigs_NG1)
gsea_NG2_none <- go_enrichment(sigs_NG2)
gsea_NG3_none <- go_enrichment(sigs_NG3)
gsea_G2G3_none <- go_enrichment(sigs_G2G3)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
